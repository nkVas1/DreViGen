{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the front end is allowed to ask the shell to do. Least privilege: this list grows when a screen genuinely needs something, never in advance.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:path:default",
    "core:webview:default",
    "core:window:default"
  ]
}
