version: 2

# The cargo and npm ecosystems are enabled in Phase 0, when the manifests land.
# Declaring them before the manifests exist only produces failing update runs.

updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: 'chore(ci)'
    labels: ['dependencies']
